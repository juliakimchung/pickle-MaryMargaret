import unittest
from margaret import *

class TestMargaret(unittest.TestCase):

	def test_can_add_message(self):

		margaret = Margaret()
		
		margaret.add_messages_to_Mary(name, message)
		self.assertEqual(message, add_messages_to_Mary[name])

if __name__ == '__main__':
	unittest.main()